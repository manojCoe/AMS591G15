% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/svm.R
\name{svmModel}
\alias{svmModel}
\title{Support Vector Machine (SVM) Model}
\usage{
svmModel(
  data,
  importance = FALSE,
  responseVariable,
  kernel = "radial",
  type = "default",
  cost = 1,
  gamma = NULL,
  epsilon = 0.1,
  degree = 3,
  coef0 = 0,
  nfolds = 10,
  k = 6
)
}
\arguments{
\item{data}{The dataset containing predictor variables and response variable.}

\item{importance}{Logical indicating whether to perform feature importance selection.}

\item{responseVariable}{The name of the response variable in the dataset.}

\item{kernel}{The type of kernel function to be used in the SVM.}

\item{type}{The type of SVM. "class" for classification and "eps-regression" for regression.}

\item{cost}{The cost parameter for the SVM.}

\item{gamma}{The kernel parameter for "radial", "polynomial", and "sigmoid" kernels.}

\item{epsilon}{The epsilon parameter for the SVM.}

\item{degree}{The degree of the polynomial kernel (only applicable if kernel = "polynomial").}

\item{coef0}{The coef0 parameter for the "polynomial" and "sigmoid" kernels.}

\item{nfolds}{The number of folds for cross-validation.}

\item{k}{The number of important features to select.}
}
\value{
A list containing the fitted SVM model and selected features.
}
\description{
Fits a support vector machine model using the e1071 package.
}
\examples{
# Load the required library
library(e1071)
# Generate sample data
set.seed(123)
x <- matrix(rnorm(100), ncol = 5)
y <- as.factor(sample(0:1, 20, replace = TRUE))
# Create a dataset
data <- data.frame(x, y)
# Fit an SVM model
svm_model <- svmModel(data = data, importance = TRUE, responseVariable = "y", kernel = "radial", type = "class", cost = 1, gamma = 0.1, epsilon = 0.1, nfolds = 5, k = 3)
# Print the selected features
print(svm_model$selectedFeatures)
# Print the SVM model details
print(svm_model$fit)
# Make predictions if needed
# predictions <- predict(svm_model$fit, newdata = x_test)
}
