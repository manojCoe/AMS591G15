% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cross_validation.R
\name{crossValidation}
\alias{crossValidation}
\title{Cross-validation Function}
\usage{
crossValidation(
  x,
  y,
  alpha = NULL,
  lambda = NULL,
  nfolds = 10,
  type = "default"
)
}
\arguments{
\item{x}{Predictor variables as a data frame or matrix.}

\item{y}{Response variable as a vector.}

\item{alpha}{Regularization parameter for elastic net regularization.}

\item{lambda}{Regularization parameter for penalized models.}

\item{nfolds}{Number of folds for cross-validation.}

\item{type}{Type of model, either "default" for regression or "class" for classification.}
}
\value{
A list containing the fitted model, best lambda value, selected features, alpha value, number of folds, and type of model.
}
\description{
Performs cross-validation for generalized linear models using the glmnet package.
}
\examples{
# Load required libraries
library(glmnet)
# Generate sample data
set.seed(123)
x <- matrix(rnorm(100), ncol = 5)
y <- rnorm(20)
# Perform cross-validation
cv_result <- crossValidation(x, y, nfolds = 5, type = "default")
# Display the results
print(cv_result)
}
